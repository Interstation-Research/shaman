{
    "name": "agents",
    "private": true,
    "type": "module",
    "scripts": {
      "clean": "rimraf dist",
      "build": "pnpm run clean && tsup",
      "start": "pnpm run build && node ./dist/main.js"
    },
    "dependencies": {
      "dotenv": "^16.4.5",
      "rimraf": "^6.0.1"
    },
    "devDependencies": {
      "ts-node": "^10.9.2",
      "tsup": "^8.3.0"
    }
  }